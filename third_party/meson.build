fmt_dep = declare_dependency (
  compile_args: [
    '-DFMT_HEADER_ONLY',
  ],
  include_directories: include_directories ('fmt/include', is_system: true),
)

mio_dep = declare_dependency (
  include_directories: include_directories ('mio/include', is_system: true),
)
